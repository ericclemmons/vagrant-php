#!/usr/bin/env php
<?php

require __DIR__.'/../vendor/autoload.php';

use Symfony\Component\Console\Application;
use Vagrant\Command\CreateCommand;

$console = new Application();

$console->add(new CreateCommand);
$console->run();
